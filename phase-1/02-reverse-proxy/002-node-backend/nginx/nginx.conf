events {}

http {
    upstream backend {
        server host.docker.internal:3000;
    }

    server {
        listen 8080;

        location / {
            proxy_pass http://backend;
        }
    }
}
