FROM nginx:latest

COPY nginx.conf /etc/nginx/nginx.conf

# Copy certificates (generated by the script)
COPY certs/nginx.crt /etc/nginx/ssl/nginx.crt
COPY certs/nginx.key /etc/nginx/ssl/nginx.key

# Expose ports
EXPOSE 8080 8443
