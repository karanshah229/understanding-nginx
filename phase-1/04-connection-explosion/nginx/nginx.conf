worker_processes auto;

events {
    worker_connections 200000;
}

http {
    server {
        listen 8080;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        location /nginx_status {
            stub_status;
        }
    }
}
